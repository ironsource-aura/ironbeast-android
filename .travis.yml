language: android
jdk: oraclejdk8
sudo: false
android:
  components:
  - android-22
  - build-tools-21.1.1
cache:
  directories:
  - "$HOME/.gradle/caches/"
script:
- "./gradlew clean build jacocoTestReport"
after_success:
- bash <(curl -s https://codecov.io/bash)
#deploy:
#  provider: script
#  script: "./gradlew bintrayUpload"
#  on:
#    tags: true
