language: android
jdk: oraclejdk8
sudo: false
android:
  components:
  - android-22
  - build-tools-22.0.0
cache:
  directories:
  - "$HOME/.gradle/caches/"
script:
- "./gradlew jacocoTestReport"
after_success:
- bash <(curl -s https://codecov.io/bash)
#deploy:
#  provider: script
#  script: "./gradlew bintrayUpload"
#  on:
#    tags: true
